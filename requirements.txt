# =============================================================================
# AP Invoice & Contract Exception Agent — Python Dependencies
# =============================================================================
# Install: pip install -r requirements.txt
# Python >= 3.11 recommended

# --- Core Framework ---
streamlit==1.37.0
pydantic==2.7.4
pydantic-settings==2.3.4
python-dotenv==1.0.1

# --- LangGraph / LangChain ---
langgraph==0.2.14
langchain==0.2.14
langchain-core==0.2.30
langchain-openai==0.1.22

# --- LLM SDKs ---
# Only the openai package is needed — OpenRouter uses the OpenAI-compatible API.
openai==1.40.0

# --- Document Processing ---
pymupdf==1.24.9          # PDF text + image extraction (fitz)
python-docx==1.1.2       # DOCX support
Pillow==10.4.0           # Image handling
pytesseract==0.3.13      # OCR wrapper (requires Tesseract binary installed)
pdf2image==1.17.0        # PDF → image for OCR (requires poppler)

# --- Data & Utilities ---
pandas==2.2.2
python-dateutil==2.9.0
ujson==5.10.0            # Fast JSON for audit JSONL
shortuuid==1.0.13        # Short unique IDs

# --- Logging & Observability ---
structlog==24.4.0

# --- Testing ---
pytest==8.3.2
pytest-asyncio==0.23.8
pytest-mock==3.14.0

# --- Dev Tools ---
ruff==0.5.5              # Linter + formatter (PEP 8)
